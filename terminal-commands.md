echo $HOME : useful for knowing which user's currently active 
echo $PATH : useful for knowing the current path in the system
git config : useful for configurating my git account
ls -la : lets me looks for directories / files with details , both visible and invisible
ls -l | grep "write something" : helps me find all files / directories containing the word I typed between doublecoats
